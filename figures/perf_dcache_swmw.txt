DLM+SW dentry cache -- transition-chain serialization strategies
================================================================
2x96 EPYC 9654, one hw thread/core (0..191), jemalloc, best-of-5, conservation-
gated.  dirs decontended to 16/writer (so the ONLY writer contention is the
transition chain, not the index buckets).  Re-run against the renamed/rebuilt
engine (userspace-rcu-txn d5c5b2de "canonical mixed front-end").  data:
scripts/dcache_swmw.csv; figure: figures/dcache_swmw.png; sweep:
scripts/run_dcache_swmw.sh.

The transition chain is a single-producer / multi-consumer list: the demote
(ENQUEUE) has one producer under the bucket lock; the folds (DEQUEUE) are many
consumers.  Four arms -- identical reader + mark; only the chain differs:
  dlm-chainlock  -DDC_CHAIN_LOCK: legacy per-host CHAIN LOCK -- demote AND folds
                 take it.  176 B.  The reference baseline.
  dlm-swmw       -DDC_CHAIN_SWMW: SW enqueue + MW dequeue -- the chain rides the
                 mixed commit as MW records (lock-free folds).  168 B.
  dlm-swmw-pad   dlm-swmw + 8 B dead padding -> 176 B, the SAME-SIZE control.
  dlm-foldlock   DEFAULT (no chain flag): SW enqueue + per-host FOLD LOCK dequeue,
                 plain chain stores, no MW descriptor.  The producer does NOT take
                 the fold lock -- only fold workers do.  176 B.

RENAME THROUGHPUT vs threads (homogeneous, rename-frac 0.5), Mrenames/s:
  thr   chainlock  swmw   swmw-pad  FOLDLOCK   foldlock/chainlock  foldlock/swmw
    1     2.71     1.53    1.56      2.41         0.89              1.58
    2     2.62     2.13    2.19      3.94         1.50              1.85
    4     3.20     3.12    3.12      6.12         1.91              1.96
    8     4.23     5.07    5.19      9.68         2.29              1.91
   16     3.30     7.68    7.51     12.39         3.75              1.61
   32     4.73    12.33   13.15     22.15         4.68              1.80
   48     4.93    17.10   18.29     28.18         5.72              1.65
   64     6.82    22.80   22.75     39.38         5.77              1.73
   96     9.22    31.59   34.08     47.97         5.20              1.52
  128    10.18    36.25   36.84     55.02         5.40              1.52
  192    18.39    51.03   51.94     68.10         3.70              1.33

RENAME THROUGHPUT vs rename fraction (48 threads), Mrenames/s:
  frac  chainlock  swmw   FOLDLOCK
   1%    4.12      7.94    7.41   (fold lock ~= swmw at 1%; both >> chain lock)
   5%    6.96     14.43   19.41
  10%    4.95     15.96   24.02
  20%    4.67     17.58   27.65
  35%    4.34     16.70   25.99
  50%    4.96     18.15   32.16   (chain lock flat/degrading; fold lock climbs)

READER PATH (frac=0 pure lookups, 48 threads), Mlookups/s -- a WASH:
  chainlock 1277   swmw 1277   swmw-pad 1279   FOLDLOCK 1276   (identical reader)

CONCLUSIONS
-----------
1. FOLD LOCK is the best of the three, and is the DEFAULT.  It nearly matches the
   chain lock uncontended (0.89x at 1 thread, vs 0.60x for the MW dequeue) and
   beats BOTH alternatives at every higher thread count -- 1.3-2.0x over the MW
   dequeue and 2.3-5.8x over the chain lock from 8 to 192 threads.  Why: the plain
   chain stores under a rarely-contended per-host lock avoid the MW dequeue's
   descriptor + call_rcu per fold (recovering the uncontended cost), while
   decoupling the producer from that lock avoids the chain lock's producer-vs-fold
   coupling (recovering the scaling).

2. The MW dequeue (dlm-swmw) is a TRADE-OFF: lock-free folds scale, but the
   descriptor cost makes it ~1.7x slower than the chain lock uncontended (1 thread).
   Its win over the chain lock is the lock-free chain, NOT the -8B: dlm-swmw (168 B)
   and dlm-swmw-pad (176 B) track each other within noise across every panel.

3. The chain lock (reference) plateaus with concurrency (its demote couples the
   producer with the folds on one per-host word) and even DEGRADES as the rename
   fraction rises.

4. Reads are a wash when uncontended (all four ~1277 Mlk/s -- identical reader).
   Under heavy concurrent rename load the mixed arms' readers run somewhat slower
   than the chain-lock readers (the mixed commit's call_rcu/reclaim traffic on the
   shared machine) -- but the mixed writers are doing far more renames.

5. The upstream canonical-front-end rename (d5c5b2de) did NOT change the shape:
   every number here is within run-to-run noise of the pre-rename sweep.
